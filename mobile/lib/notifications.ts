// Push notifications are disabled until Firebase/FCM is configured on expo.dev.
// Re-enable by installing expo-notifications + expo-device and wiring up FCM credentials.
export async function registerForPushNotifications(): Promise<string | null> {
  return null;
}
